name: acmejax
channels:
  - defaults
  - conda-forge
dependencies:
  - pip
  - python==3.7
  - pip:
    - pip
    - reverb
    - setuptools
    - wheel
    - rlax
    - tensorflow-probability==0.13
    - 'dm-acme'
    - 'dm-acme[jax]'
    - 'dm-launchpad'
    - 'dm-acme[envs]'



# conda env create -f cpu.yaml
# conda remove --name acmejax --all