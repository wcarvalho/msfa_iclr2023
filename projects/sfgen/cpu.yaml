name: acmejax
channels:
  - defaults
  - conda-forge
dependencies:
  - pip
  - python==3.7
  - pip:
    - jax[cpu]
    - pip
    - reverb
    - setuptools
    - wheel



# conda env create -f cpu.yaml
# conda remove --name sfgen --all